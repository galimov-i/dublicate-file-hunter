# 🕵️ Duplicate File Hunter (Охотник за Дубликатами)

Высокопроизводительный инструмент командной строки для поиска дубликатов файлов в дереве каталогов на основе содержимого (MD5 хеш). Создан с использованием Python и Rich для красивого интерфейса в терминале.

## Возможности

- **Оптимизация производительности**: Использует алгоритм "двух проходов" для минимизации ввода-вывода и использования ЦП.
  - **Проход 1 (Фильтр по размеру)**: Быстро группирует файлы по размеру. Файлы с уникальным размером сразу отбрасываются.
  - **Проход 2 (Проверка хеша)**: Вычисляет MD5 хеши только для файлов, имеющих одинаковый размер.
- **Эффективность памяти**: Читает файлы частями, что позволяет обрабатывать большие файлы без переполнения оперативной памяти.
- **Красивый интерфейс**: Использует `rich` для отображения прогресс-баров, цветного вывода и сводных таблиц.
- **Кроссплатформенность**: Работает на Windows, Linux и macOS.

## Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/galimov-i/dublicate-file-hunter.git
    cd dublicate-file-hunter
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

## Использование

Запустите скрипт, указав путь к каталогу, который нужно просканировать:

```bash
python3 dedupe.py /путь/к/папке
```

Если путь не указан, сканируется текущий каталог:

```bash
python3 dedupe.py
```

## Как это работает

1.  **Сканирование**: Рекурсивно проходит по указанному каталогу.
2.  **Фильтрация**: Находит файлы с одинаковым размером.
3.  **Хеширование**: Вычисляет MD5 хеши для потенциальных дубликатов.
4.  **Отчет**: Отображает подробную таблицу дубликатов и общий объем места, которое можно освободить.

## Требования

- Python 3.10+
- Библиотека `rich`

## Лицензия

MIT
